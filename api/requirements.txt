# Minimal dependencies for Backend API inference
flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
# xgboost is removed to fit within Vercel's 500MB limit. 
# It is only needed if the trained model.pkl was an XGBoost model.
# Current best model is 'Gradient Boosting' (sklearn).
# pandas is omitted as backend/predict.py uses numpy arrays directly
